# MihomoTui 用户体验优化方案

针对 MihomoTui 的现有功能和用户界面，我们从“用户为导向”的角度出发，结合现代终端 UI 最佳实践，提出以下改进建议。总体设计应遵循终端界面“文本优先、简洁稳定”的原则【19†L274-L282】，每个界面都要让用户明确“我在哪、发生了什么、下一步怎么办”【19†L299-L301】，并提供清晰的操作提示和帮助【24†L181-L189】。

## 设计原则

- **以文本为本、简洁稳定**：界面布局采用单栏阅读为主，标签和值对齐显示，避免冗余元素【19†L289-L297】。例如固定侧边栏、标题和状态栏位置，使布局在不同终端尺寸下保持稳定【19†L289-L297】。  
- **可识别与对齐**：信息分区要清晰，保持每个页面内容对齐有序【19†L289-L297】。界面颜色应语义化（绿色表示成功，黄色警告，红色错误，蓝色信息等【19†L317-L323】），并配合文字提示（如“OK”、“WARN”、“ERROR”）避免仅靠颜色区分状态【19†L327-L330】。同时确保高对比度和无色环境下可辨识【19†L369-L374】。  
- **可发现性与帮助**：界面应集成上下文帮助和提示【24†L181-L189】。例如在侧边栏、表单或按钮上提供快捷键提示和使用说明，使用户无需记忆即可发现功能【24†L181-L189】。全局帮助面板（`?`键）可显示当前页面所有快捷键和操作指南，并在错误或空状态时给出明确提示【24†L181-L189】【19†L299-L301】。  
- **安静高效**：平时界面尽量安静不打扰用户，只有在状态变化或错误时突出提醒【19†L299-L304】。进度或延迟测试等需要耗时操作时，可用简洁的加载指示（如进度条或微型动画），但避免过度闪烁【19†L343-L350】。

## 各页面功能优化

下面针对 MihomoTui 的7个页面（仪表盘、代理、连接、配置、日志、规则、设置）逐一提出优化建议。

### 仪表盘（Dashboard）

- **突出关键信息**：在页面顶部或明显区域显示当前代理模式（如全局/规则模式）和 TUN 开关状态，使用彩色图标或标签表明开/关【19†L317-L323】。切换模式或 TUN 时应有实时反馈（弹窗确认或状态提示）。  
- **流量统计可视化**：显示实时上下行流量和连接数。建议采用柱状图或折线图（ASCII 绘图）显示短期变化，便于快速扫描。当数据正常时，图表要低调；若连接异常或超阈，可用颜色强调高峰【19†L317-L323】【19†L327-L330】。  
- **简洁布局**：仪表盘界面应只展示概览信息，保持一列布局【19†L289-L297】。例如第一列是连接统计（当前连接数、断开数），第二列是流量统计图，第三列显示模式控制。如果空间不足，可通过折叠或切换视图来详细查看。  
- **动作提示**：仪表盘底部或状态栏显示简要操作提示，如“按“T”切换模式”、“按“TAB”切换焦点”等，确保用户知道可执行的操作【19†L299-L301】。  

### 代理管理（Proxies）

- **两列清晰展示**：左侧列出“提供者列表”或订阅源，右侧列出“代理组”。当前选择的提供者或组应高亮标记。选中后下方显示其代理节点表格【19†L289-L297】。  
- **节点表格设计**：代理节点表格按列显示名称、延迟、负载等信息。延迟测试结果用数字加单位（ms），并配合颜色或符号（如 ✓/⚠/✗）指示状态，避免单靠颜色【19†L327-L330】。表格列标题对齐，支持列宽自适应或可调节。  
- **搜索与筛选**：在代理列表顶部提供搜索输入框，可按名称或类别过滤节点【19†L299-L301】。例如输入“Google”只显示节点名称包含“Google”的行。  
- **节点切换优化**：选中节点后，回车键确认切换并即时更新显示。切换请求失败时，在状态栏提示错误原因，成功时显示绿色成功消息【19†L327-L330】。切换节点时可弹出确认或进行二次操作，以防误操作。  
- **延迟测试交互**：按空格或点击按钮可启动对当前组或节点的延迟测试。测试过程在节点行显示进度（如旋转符号或“测试中...”文本），结束后自动填充延迟值。避免长时间动画，测试完成后用不同颜色或符号标注延迟等级【19†L317-L323】【19†L343-L350】。  

### 连接监控（Connections）

- **数据表格展示**：列出所有活跃连接，每列包括本地地址、远程地址、传输数据量、持续时长等。表格列对齐，字段宽度适中，超长内容可截断并可滚动查看。标题栏可点击排序（如按流量大小或时长排序）。  
- **高亮异常**：对于数据异常或长时间未响应的连接，可用醒目颜色突出显示（例如超过某个时长标红）【19†L317-L323】。同时在状态栏显示总连接数和异常数量，提示用户注意。  
- **搜索与过滤**：顶部提供搜索框，可通过IP、端口或协议过滤连接。也可以提供多选过滤器，如协议类型（TCP/UDP）或状态（已关闭/等待）。通过 **/** 键快速聚焦搜索框，提高可发现性【19†L299-L301】。  
- **操作反馈**：允许用户选中一行后按回车或快捷键关闭该连接。关闭后自动刷新列表，并在状态栏提示操作结果（成功或失败信息）。如果列表较长，可提供“关闭所有”或“关闭筛选结果”的批量操作。  

### 配置（Config）

- **格式化表单**：配置页面应有表单输入框供填写 API 地址和 Secret。字段前置标签对齐左侧，示例文本（placeholder）展示格式提示，如 `http://127.0.0.1:9090`【19†L289-L297】。  
- **连接测试功能**：新增“测试连接”按钮，在用户输入 API 地址后立即验证连通性。测试时显示转轮或短暂提示，成功或失败后弹出提示信息，并更新状态栏【24†L181-L189】。  
- **输入校验**：提交时检查地址格式（必需包含 http:// 或 https://），Secret 可为空或长度检查。出错时在对应字段下方显示错误信息，而不是一整页弹窗。  
- **保存与提示**：保存配置后给出反馈（如状态栏绿色 “保存成功”），并提醒用户重启应用以生效。必要时，可在保存后自动重启或提示要重启。  

### 规则（Rules）

- **清晰的规则表格**：规则页面应将类型（TYPE）、加载动作（PAYLOAD）和代理（PROXY）分列显示，列宽对齐整齐【19†L289-L297】。根据规则数量，可分页或支持滚动加载。  
- **快速搜索**：在规则表头或上方提供搜索框，用户输入关键字时实时过滤匹配的规则【19†L299-L301】。例如输入“IP-CIDR”可只显示类型为IP-CIDR的规则。  
- **规则状态标识**：当前应用中的活动规则可加粗或用颜色标记，方便区分默认/生效规则和非生效项。  
- **可编辑性**：如果 Mihomo API 支持，可考虑允许右键或快捷键编辑规则（例如启用/禁用某条规则）。至少要有“复制规则内容”功能，方便用户快速定位或修改规则文件。  

### 日志（Logs）

- **实时流与暂停**：日志页面以流式滚动方式显示最新日志，支持上下滚动查看历史内容。提供“暂停滚动”开关，用户滚动或点击暂停时停止自动滚动【19†L299-L301】；点击继续滚动按钮后恢复自动更新。  
- **级别过滤**：界面上提供日志级别过滤选项（如只显示 WARNING/ERROR），选中后仅显示相应级别的日志行【19†L317-L323】。等级信息在每行日志前缀中显示（如 `[INFO]`）。  
- **搜索功能**：顶部提供关键词搜索框，可高亮并筛选包含指定文本的日志行【19†L299-L301】。  
- **突出错误**：ERROR、WARN 等关键字行可用红/黄文字或背景色标出，但同时保留文本形式的“ERROR”标签【19†L327-L330】。正常信息用默认颜色显示，避免视觉噪音过大。  
- **清理与导出**：提供清空当前日志显示的功能，以及将日志保存到文件的选项，以便离线查看或排查问题。  

### 设置（Settings）

- **分区管理**：设置页面应分为几个分区，如“配置备份/恢复”、“订阅管理”、“代理组管理”等。每个分区使用不同标题和边框分隔，避免将所有选项堆在一起【19†L289-L297】。  
- **备份/恢复**：为配置备份和恢复提供按钮，用户点击后提示备份保存路径或读取文件。完成操作后给出成功/失败提示。  
- **订阅导入**：提供输入框让用户粘贴订阅 URL 和对应的名称，带“添加”按钮。URL 输入完成后可自动验证格式，有错误提示；添加成功则在下方列表显示订阅来源名称。【24†L181-L189】用户导入后可查看导入结果并自动重载配置。  
- **代理组管理**：列出现有自定义组（名字、类型和对应的成员），并提供输入框和按钮新增组。新增时按类型选择（select、url-test、fallback、load-balance），填写名称和要使用的现有提供者。对删除组操作要求二次确认【24†L181-L189】。  
- **交互提示**：所有设置的操作（添加、删除、备份等）完成后，都应即时反馈结果，并保持界面数据同步更新，避免需要重启才能看到效果。  

### 导航与快捷键

- **一致的导航体验**：侧边栏列出所有页面名称（如“仪表盘”、“代理”等），每个名称后可标注对应快捷键（例如 `F1`、`Alt+D`）。确保当前页高亮，用户一眼可知所在位置【19†L299-L301】。  
- **全局帮助提示**：在侧边栏或状态栏持续显示可用快捷键（如 `ESC` 返回侧边栏，`?` 弹出帮助）。按 `?` 显示页面帮助时，要列出本页的所有交互方式，避免用户遗漏功能【24†L181-L189】。  
- **键位冲突最小化**：避免给不同功能设置冲突的快捷键，必要时允许用户自定义或通过配置文件更改常用按键。  
- **鼠标与键盘并行**：既然已经支持鼠标，确保所有按钮和列表也能通过键盘操作（选中后回车、方向键滚动等）【25†L378-L386】。  

### 视觉一致性与布局

- **统一风格**：全局使用统一的边框、标题和字体样式。利用 tview 的布局组件（如 Flex、Grid）确保界面在调整终端大小时仍合理排布，不会溢出或重叠【19†L289-L297】。  
- **对齐与间距**：表格列、表单标签和值保持对齐。各组件之间留出适当空白，让界面看上去不拥挤，且便于快速扫描【19†L289-L297】。  
- **主题支持**：可考虑提供浅色/深色主题切换，保持语义颜色的一致性。主题切换后应保留相同的可读性和高对比度。  

## 实施要点与测试策略

- **优先级划分**：首先保证现有功能（代理切换、连接显示等）正确无误，其次完善用户引导（如帮助提示、操作确认）；接着优化搜索过滤、布局与配色；最后添加可选功能（主题、日志导出等）。逐步迭代，避免一次性改动过多带来风险。  
- **代码健壮性**：对后端 API 调用要加错误处理和超时机制，界面操作异常时给出明确提示，避免崩溃或挂起。按照“鲁棒性”的指导【24†L219-L227】，对每次可能出错的操作提供友好反馈。  
- **多终端测试**：在不同分辨率和配色方案下测试界面，确保布局自适应，颜色在浅/深背景下都清晰可见。【19†L369-L374】尤其要验证在无色模式下的可用性，例如用字符标识状态而非仅用颜色区分。  
- **回归测试**：每次改动后都需验证原有功能未受影响（如基本的代理切换、流量统计等）。可编写简单的端到端脚本模拟用户操作，也可进行手工测试检查交互流程。  

## 总结

本优化方案以用户体验为核心，对 MihomoTui 的界面布局、交互逻辑和功能辅助进行了全面考虑。主要目标是**简洁可见、操作直观、安全可靠**——确保用户无需阅读大量文档就能快速上手，界面在正常操作下保持安静，只有在需要注意时才突出显示【19†L299-L301】。通过引入搜索过滤、操作确认、实时反馈、帮助提示等手段，并参考终端 UI 设计准则【19†L274-L282】【24†L181-L189】，可大幅提升使用便利性和满意度。上述建议可分阶段实施，先易后难，不断收集用户反馈并改进，最终实现一个功能齐全、用户友好的 MihomoTui 终端管理界面。

**参考资料：** 终端界面设计指南【19†L274-L282】【19†L289-L297】【19†L299-L301】、命令行界面用户体验原则【24†L181-L189】（提供发现性和帮助指引）。